{
  "name": "scandi-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -r node_modules growlers/node_modules",
    "docker:build": "yarn clean && docker build . -t growlers"
  },
  "workspaces": [
    "extension",
    "growlers"
  ]
}
